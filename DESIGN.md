# DESIGN.md

이 저장소에는 성격이 다른 두 화면이 있다. 둘은 토큰도 규칙도 다르다. 섞지 않는다.

| | 공개 사이트 `(site)` | 어드민 `(admin)` |
| --- | --- | --- |
| 보는 사람 | 사건이 생겨 검색해 들어온 의뢰인 | 변호사 본인 (혼자, 매일은 아님) |
| 디자인의 역할 | 설득한다. 디자인이 곧 상품 | 물러난다. 디자인은 업무를 돕는 도구 |
| 색 출처 | `site_settings` (어드민에서 변경) | `admin.css` 고정 토큰 |
| 변수 | `--background`, `--foreground`, `--primary`… | `--a-*` |
| 범위 | `.site-theme` | `.admin-shell` |

어드민 미리보기 안에서는 사이트 변수가 그대로 산다. 그래서 두 변수 이름이 겹치면 안 된다. 어드민 변수에 `--a-` 접두어가 붙은 이유다.

---

## 어드민

### 방향: 클리닉

변호사가 처음 열어도 헤매지 않는 것이 1순위다. 밀도보다 확실함을 택한다.

- 간격과 타깃을 키운다. 입력칸·버튼 높이 36px
- 누를 수 있는 것은 버튼처럼 보인다. 글자 링크로 된 동작을 만들지 않는다
- 색은 상태를 말할 때만 쓴다. 장식으로 쓰지 않는다
- 모션은 150ms 내외. 상태 변화만 알린다. 등장 애니메이션은 넣지 않는다

### 토큰

정의는 `src/app/(admin)/admin/admin.css`. 여기 있는 것만 쓴다. **Tailwind 기본 팔레트(`zinc-400`, `emerald-50` …)를 어드민 화면에 직접 쓰지 않는다.** 필요한 색이 없으면 토큰을 먼저 추가한다.

| 역할 | 토큰 | 쓰는 곳 |
| --- | --- | --- |
| 앱 배경 | `--a-bg` | 화면 바닥 |
| 카드·목록 | `--a-surface` | 흰 면, 사이드바 |
| 패널 머리 | `--a-surface-2` | 보조 패널 |
| 펼친 영역 | `--a-surface-3` | 편집 폼, 호버 |
| 본문 | `--a-ink` | 제목·값. 흰 바탕 대비 13:1 |
| 보조 | `--a-ink-2` | 라벨·설명. 6.3:1 |
| 흐림 | `--a-ink-3` | 경로·개수 같은 부차 정보. 4.6:1 |
| 강조 | `--a-accent` | 기본 동작 버튼, 현재 선택, 포커스 |
| 상태 | `--a-ok` / `--a-warn` / `--a-danger` | 노출중 / 저장 전 / 삭제 |

강조색과 상태색은 **다른 역할**이다. 저장 버튼이 청록이라고 해서 "성공"을 뜻하지 않는다.

본문 대비는 4.5:1, 큰 글씨는 3:1을 넘겨야 한다. 회색을 더 흐리게 하고 싶으면 그 전에 이 수치를 확인한다.

### 컴포넌트 어휘

같은 뜻이면 모든 화면에서 같은 모양이어야 한다. 새 클래스를 만들기 전에 이 표를 본다.

| 클래스 | 뜻 |
| --- | --- |
| `a-btn a-btn-primary` | 이 화면의 기본 동작. **화면당 하나** |
| `a-btn a-btn-default` | 보통 동작 (수정, 취소) |
| `a-btn a-btn-danger` | 되돌릴 수 없는 동작 (삭제) |
| `a-btn a-btn-quiet` | 보조 동작 (로그아웃, 닫기) |
| `a-btn-sm` | 목록 안처럼 좁은 자리에 덧붙임 |
| `a-input` / `a-select` / `a-textarea` | 입력. 호버·포커스·비활성 상태 포함 |
| `a-field` + `a-label` | 라벨 + 입력 한 쌍 |
| `a-badge a-badge-ok/off/warn` | 상태 표시 (노출중 / 숨김 / 저장 전) |
| `a-chip` | 분류 표식. 누를 수 없음 |
| `a-tab[data-active]` | 페이지 선택처럼 누르는 칩 |
| `a-card` / `a-panel` | 면 |
| `a-list` + `a-row` | 목록. 순서·내용·상태·동작 고정 격자 |
| `a-editor` | 행을 펼쳤을 때의 편집 영역 |
| `a-empty` | 아무것도 없을 때 |

`a-row`가 고정 격자인 이유: 제목 길이에 따라 상태 배지와 버튼이 좌우로 흔들리면 눈이 매번 위치를 다시 찾아야 한다.

### 상태는 빠뜨리지 않는다

버튼·입력을 새로 만들면 기본 / 호버 / 포커스 / 비활성 / (해당되면) 로딩까지 만든다. 포커스는 `--a-ring` 3px. 키보드로만 쓰는 사람이 지금 어디 있는지 보여야 한다.

빈 화면은 "없습니다"로 끝내지 않고 다음에 할 일을 알려 준다. — "이 페이지에 등록된 블록이 없습니다. 위의 블록 추가로 시작하세요."

### 하지 않는 것

- 장식용 그라데이션·글래스·페이지 진입 애니메이션
- 제목용 디스플레이 폰트. 어드민은 한 서체로 간다
- 비활성 상태에 진한 색
- 모달을 먼저 떠올리기. 인라인으로 풀 수 있으면 인라인으로 (미리보기를 모달이 아니라 분할로 둔 이유)

---

## 공개 사이트

색·서체는 코드가 아니라 `site_settings`에 있고 어드민에서 바뀐다. 그래서 컴포넌트는 값을 고정하지 않고 변수를 읽는다.

- 색: `var(--background)`, `var(--foreground)`, `var(--primary)`, `var(--accent)`, `var(--border)`, `var(--hero-background)`
- 서체: `var(--font-site-sans)`, `var(--font-site-heading)`
- 라이트 모드 고정. 인쇄·캡처로 공유되는 일이 많아 시스템 설정에 따라 배경이 바뀌지 않는 편이 낫다
- 어드민 미리보기에서 그대로 렌더되므로, 뷰포트 단위(`vh`)로 높이를 잡을 때는 `--hero-min-height`처럼 밖에서 덮을 수 있게 둔다

`data-field` 속성은 어드민 미리보기가 "지금 입력 중인 칸"을 찾는 표식이다. 공개 사이트 동작에는 영향이 없다. 카피 요소를 새로 만들면 같은 규칙으로 붙인다 — 최상위 필드는 `title`·`subtitle`, `content` 안의 값은 `content.<키>`.
